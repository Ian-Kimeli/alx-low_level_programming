#ifndef MACRO_LIKE_H
#define MACRO_LIKE_H
/**
 * File: 3-function_like_macro
 *
 * @x: input value (an int)
 *
 * Desc: hearder file compute absolute value
 */
#define ABS(x) ((x) < 0 ? -(1) * (x) : (x))

#endif
